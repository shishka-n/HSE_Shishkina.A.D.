def isMonotonic(A):
    x, y = [], []
    x.extend(A)
    y.extend(A)
    x.sort()
    y.sort(reverse=True)
    if(x == A or y == A):
        return True
    return False

A = [6, 5, 4, 4]
B = [1, 2, 2, 3]
C = [1, 3, 2]
D = [9, 10, 12]
E = [51, 73, 77, 78]
F = [91, 90, 89]

print(isMonotonic(A))
